name: badness

on:
  pull_request:
  push:
    branches: [main]

jobs:
  badness:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: jolars/badness-action@v1
        with:
          format: "false"
